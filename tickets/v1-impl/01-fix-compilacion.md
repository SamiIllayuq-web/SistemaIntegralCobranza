# 01 — Fix de compilación

**What to build:** El proyecto compila sin errores (`mvn compile` pasa limpio).

**Blocked by:** Ninguno — puede empezar inmediatamente.

**Status:** ready-for-agent

- [ ] `mvn compile` pasa sin errores
- [ ] Los errores de Lombok están resueltos (annotation processor configurado correctamente)

---

## Notes

El ticket `tickets/003-compilacion-lombok.md` ya existe con el diagnóstico detallado del problema de Lombok.
